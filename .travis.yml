language: java

dist: trusty
sudo: required

jdk:
  - oraclejdk8
  
script: 
  - mvn clean install

cache:
  directories:
    - '$HOME/.sonar/cache'
    - '$HOME/.m2'

notifications:
email: false
branches:
    only:
    - master
deploy:
  provider: heroku
  api_key: $HEROKU_API_KEY
app: cipote
